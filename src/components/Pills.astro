---
const { class: className = '', ...props } = Astro.props;
---

<a
    class={`${className} inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-white/90 bg-white/5 border border-white/10 rounded-lg hover:bg-white/10 hover:border-white/20 hover:text-white transition-all duration-200 backdrop-blur-sm`}
    {...props}
    target="_blank"
    rel="noopener noreferrer"
>
    <slot/>
</a>
